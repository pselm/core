{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build",
    "clean": "rimraf output && rimraf .pulp-cache",
    "docs": "pulp docs",
    "test": "pulp test",
    "simple": "pulp run -m Examples.Simple -I examples"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "pulp": "^12.0.0",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  }
}
